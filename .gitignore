node_modules/
# NOTE: dist/*.min.js is intentionally COMMITTED. The Cloudflare Workers
# deploy (wrangler.jsonc → assets.directory ".") serves the repo as static
# files with no build step, so the bundles referenced by the HTML must exist
# in git. Run `npm run build` and commit dist/ whenever js/ source changes.
dist/*.map

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
supabase/.temp/
.claude/worktrees/

# macOS Finder metadata
.DS_Store
**/.DS_Store
