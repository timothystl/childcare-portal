function spEl(t){return document.getElementById(t)}function spEsc(t){return String(t!=null?t:"").replace(/[&<>"']/g,e=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"})[e])}function spMoney(t){return"$"+(Number(t)||0).toLocaleString("en-US",{minimumFractionDigits:2,maximumFractionDigits:2})}function spDate(t,e){return t?new Date(String(t)+"T12:00:00").toLocaleDateString("en-US",e||{month:"long",day:"numeric",year:"numeric"}):"\u2014"}function spFail(t,e){const n=spEl("spState");n.className="sp-state sp-state-fail",n.innerHTML=`<strong>${spEsc(t)}</strong><p>${e}</p>`,n.classList.remove("hidden"),spEl("spPage").classList.add("hidden"),spEl("spBarNote").textContent="Not issued"}function spParams(){const t=new URLSearchParams(location.search);return{family:t.get("family"),from:t.get("from"),to:t.get("to")}}function spMissingProviderFields(t){return[["legal_name","Provider name"],["address","Address"],["ein","Employer Identification Number (EIN)"]].filter(([n])=>!String((t==null?void 0:t[n])||"").trim()).map(([,n])=>n)}function spUncoveredMonths(t){return(t||[]).filter(e=>(e.care_days||0)>0&&(e.payments||0)===0)}function spMonthLabel(t){const[e,n]=String(t).split("-").map(Number);return new Date(e,n-1,1).toLocaleDateString("en-US",{month:"long",year:"numeric"})}function spRender(t,e){var c,p,h;const n=t.provider||{},a=t.family||{};spEl("spOrgName").textContent=n.legal_name||"",spEl("spOrgSub").textContent=n.org_line||"",spEl("spOrgAddr").textContent=[n.address,n.phone,n.email].filter(Boolean).join(" \xB7 ");const i=String(e.from).slice(0,4)===String(e.to).slice(0,4)&&String(e.from).endsWith("-01-01")&&String(e.to).endsWith("-12-31");spEl("spTaxYear").textContent=i?String(e.from).slice(0,4):`${spDate(e.from,{month:"short",day:"numeric"})} \u2013 ${spDate(e.to,{month:"short",day:"numeric",year:"numeric"})}`,spEl("spTitle").textContent=i?"Year-End Childcare Statement":"Childcare Statement",spEl("spLede").textContent=i?"For claiming the Child and Dependent Care Credit. Provide this statement to your tax preparer or attach to Form 2441.":"For dependent-care reimbursement. Provide this statement to your employer or benefits administrator.",spEl("spProvName").textContent=n.legal_name||"",spEl("spProvAddr").textContent=n.address||"",spEl("spProvEin").textContent=n.ein||"",String(n.license_no||"").trim()?spEl("spProvLic").textContent=n.license_no:spEl("spProvLicRow").remove();const u=[a.parent_name,a.parent2_name].filter(Boolean).join(" and ");spEl("spParent").textContent=u||a.parent_email||"";const s=t.children||[];spEl("spChildLabel").textContent=s.length===1?"Child":"Children",spEl("spChildren").innerHTML=s.length?s.map(r=>`${spEsc(r.child_name)} <span class="sp-kid-days">(${r.days} ${r.days===1?"day":"days"})</span>`).join("<br>"):'<span class="sp-none">No care days recorded in this period</span>';const d=(c=t.period)==null?void 0:c.first_care_date,l=(p=t.period)==null?void 0:p.last_care_date;spEl("spCarePeriod").textContent=d&&l?`${spDate(d)} \u2013 ${spDate(l)}`:`${spDate(e.from)} \u2013 ${spDate(e.to)}`,spEl("spTotalPaid").textContent=spMoney(t.total_paid),spEl("spTotalPaidSub").textContent=`${spDate(e.from,{month:"short",day:"numeric"})} \u2013 ${spDate(e.to,{month:"short",day:"numeric",year:"numeric"})}`,spEl("spTotalDays").textContent=String((h=t.days_of_care)!=null?h:0),spEl("spTotalDaysSub").textContent="days attended in this period",spEl("spNote").textContent="This statement reflects amounts paid directly to the provider named above for qualifying childcare expenses during the stated period. It does not constitute tax advice \u2014 please consult your tax preparer regarding eligibility for the Child and Dependent Care Credit (IRS Form 2441).";const m=t.payments||[];m.length?spEl("spPaymentsTable").innerHTML='<tr><th>Date</th><th>Method</th><th class="sp-right">Amount</th></tr>'+m.map(r=>`<tr>
                    <td>${spEsc(spDate(r.payment_date,{month:"short",day:"numeric",year:"numeric"}))}</td>
                    <td>${spEsc(r.payment_method||"\u2014")}</td>
                    <td class="sp-right">${spEsc(spMoney(r.amount))}</td>
                </tr>`).join("")+`<tr class="sp-total-row"><td colspan="2">Total</td>
                 <td class="sp-right">${spEsc(spMoney(t.total_paid))}</td></tr>`:spEl("spPaymentsBlock").remove(),spEl("spIssued").textContent="Issued "+spDate(new Date().toLocaleDateString("en-CA",{timeZone:"America/Chicago"})),spEl("spContact").textContent=[n.email,n.phone].filter(Boolean).join(" \xB7 "),spEl("spSignName").textContent=n.signature_name||n.legal_name||"",spEl("spState").classList.add("hidden"),spEl("spPage").classList.remove("hidden"),spEl("spBarNote").textContent="One statement \xB7 letter \xB7 keep for your records",spEl("spPrint").hidden=!1}async function spLoad(){const t=spParams();if(!t.family||!t.from||!t.to){spFail("This link is incomplete.","A statement needs a family and a date range. Open it again from the Documents tab or the admin portal.");return}let e;try{e=await fetchFamilyCareStatement(t.family,t.from,t.to)}catch(o){spFail("The statement could not be loaded.",spEsc(o.message||String(o)));return}if(!e){spFail("This statement is not available to you.","Either the sign-in does not belong to this family, or the family no longer exists.");return}const n=spMissingProviderFields(e.provider);if(n.length){spFail("The provider tax details are not on file yet.","A statement is only usable on Form 2441 once it carries the center\u2019s legal name, address and EIN. Still to enter: <strong>"+n.map(spEsc).join(", ")+"</strong>.<br><br>An administrator adds these once, in the admin portal under Settings \u2192 Provider tax details.");return}const a=spUncoveredMonths(e.coverage);if(a.length){spFail("This period is not fully reconciled, so the statement was not issued.","These months have care days on record but no payment recorded against them, which means the total below them would be short: <strong>"+a.map(o=>spEsc(spMonthLabel(o.month))+" ("+o.care_days+" care "+(o.care_days===1?"day":"days")+")").join(", ")+"</strong>.<br><br>Record those payments in Finance \u2192 Ledger, or choose a period that ends before them. A statement that understates what a family paid is worse than one they have to wait for.");return}spRender(e,t)}document.addEventListener("DOMContentLoaded",()=>{var t;(t=spEl("spPrint"))==null||t.addEventListener("click",()=>window.print()),spLoad()});
