{
  "name": "childcare-portal",
  "version": "2.13.19",
  "private": true,
  "description": "Timothy Lutheran MDO Registration Portal — build tooling",
  "scripts": {
    "bump": "node scripts/bump-patch.js",
    "build": "node scripts/build.js",
    "build:watch": "node scripts/build.js --watch",
    "test": "node scripts/check-migrations.js && node js/tests/business-logic.test.js && node --test test/contracts/*.test.js test/shared/*.test.js",
    "migrations:check": "node scripts/check-migrations.js",
    "migrations:snapshot": "node scripts/snapshot-migrations.js"
  },
  "devDependencies": {
    "ajv": "8.20.0",
    "ajv-formats": "3.0.1",
    "esbuild": "^0.24.2"
  },
  "dependencies": {
    "dompurify": "^3.4.15",
    "tinymce": "^8.9.1"
  }
}
